# guanyisearch 项目交接

> 更新日期：2026-09-22
> 本文用于续接产品与代码工作；不记录凭据、个人邮箱、基础设施或其他内部运营信息。

## 2026-09-22 阶段交接更新：研究者端、账号链路与邮件体验

### 本阶段完成项

- 研究者端工作区已收敛为独立的品牌界面：左上角仅保留透明大 G，工作区保留浅薄荷绿、暖白、墨绿和细线层级，不复制外部产品的品牌、文案或导航结构。
- 工作区首页完成三条明确入口：新建研究、从 AI 开始、问卷设计。欢迎语采用首页展示字体，姓名与问候语同一行但以不同颜色区分；不再使用右上角字母头像作为主视觉。
- 引导页、研究者登录与找回密码的路由已按参与者端和研究者端分流；研究者注册不再因为历史组织类型/地区校验而在前端看似完成、后端却拒绝创建。
- 邮箱验证码链路已修正为发送后可验证、注册提交时再消费；避免预检查提前作废验证码。验证码过期、重复使用和重新发送仍保持明确提示。
- 欢迎、验证码与密码重置邮件已统一为完整白色画布：透明 G 与 `guanyisearch` 字标、首页同类展示字体、自然的一句跨区域市场研究定位、横向满幅欢迎图片、极简验证码分组与细分隔线。移除了厚重卡片、装饰性墨绿横条和大写胶囊框。
- 品牌文本统一使用 `guanyisearch`（小写）；浏览器默认标题与描述改为：`guanyisearch — Cross-Regional Market Research`，并以“跨区域市场研究、真实声音与更清晰决策”为公开定位。
- 邮件模板语法与模板完整性检查已通过。后端邮件模板提交已推送到远端生产分支。

### 发布与运行确认

- 后端远端分支已包含本轮邮件模板、验证码校验与小写品牌修正。运行环境是否已自动拉取并重启，必须用一次真实注册邮件或部署状态确认；不能仅凭代码推送推断邮件服务已切换。
- 前端本地已准备好欢迎邮件照片、透明 G、字标资源以及浏览器标题/描述提交；本次向前端远端推送被权限拒绝，因此这些前端资源和元信息**尚未确认发布**。后续必须在获得正确仓库写入授权后，先推送前端，再用生产浏览器确认标题、描述和邮件图片可访问。
- 未把令牌、收件人、服务器位置、供应商或其他运行配置写入仓库、邮件文案或本文档。

### 本阶段验证

- 后端：`node --check` 覆盖邮件模板；`node scripts/verify-email-templates.js` 通过。
- 邮件预览：欢迎、验证码、找回密码分别在浏览器渲染检查。欢迎邮件的图片已横向贯穿邮件；验证码不再使用独立大色块框；重置邮件只保留必要说明与明确操作。
- 前端：标题和描述更新已提交到本地分支；在前端远端写入恢复前，不能声称其已上线。

### 下一阶段：平台功能闭环与 AI 能力

**原则**：先让真实用户任务顺畅闭环，再把 AI 放进明确、可审查、可撤销的辅助位置。AI 不应伪造研究结论、替用户提交研究、代替人工审核，也不应在没有明确输入和告知时处理个人资料。

1. **账号与研究者工作台闭环**
   - 真实注册、验证、登录、密码重置、退出及错误状态逐条做生产验收。
   - 补齐研究项目、简报、问卷设计、提交审核、报价与结果入口之间的返回路径、空状态和通知跳转。
   - 优先解决“前端显示成功但后端状态不一致”的问题，所有状态以服务端真实记录为准。
2. **参与者机会与结果闭环**
   - 继续以真实机会为准：匹配、报名、状态变化、完成记录和结果可见性必须形成连续流程。
   - 不展示演示供给、虚构样本、虚构奖励或未经授权的研究结果。
3. **AI：从“研究简报辅助”开始**
   - 第一项能力为将研究者提供的自然语言目标整理为可编辑的研究简报草案：研究问题、目标受众、建议方法、需要补充的信息和风险提示。
   - 输出永远是草案，不自动创建、提交、发布问卷或对外发送；研究者必须审核并明确确认。
   - 在接入模型服务前，先完成 API 边界、输入校验、错误反馈、草案版本记录和人工确认交互；接入后再进行真实调用、速率保护与审计。
4. **发布验收**
   - 每个版本固定完成：前端构建、后端相关流程检查、桌面与移动端关键路径、一次真实账号流程和一次真实邮件投递检查。
   - 前端、后端与运行环境的发布状态分别记录；只有三者均已验证，才能称为“生产已上线”。

## 当前产品状态

### 首页结构

- `/` 为双层首页：第一屏为深绿、蓝绿与暖棕渐变画布上的白色编辑式 Hero 面板，第二屏开始保留 Atlas 信息页与后续品牌内容。
- Hero 使用三行展示标题（简体中文为“打破鸿沟。／每个人的声音／携带向前。”）与“参与深入的研究，分享你的发现，帮助将生活经验转化为更清晰的决策。”；仅保留一个 `Join us` 行动入口。
- Hero 面板下方的圆角媒体窗口复用现有 `skyReflection` 背景图，不新增视频或外部素材。
- 顶部导航组件和交互保持不变：首屏透明叠加，滚动后保持固定且转为可读的实底导航。
- 第二页左侧已改为纵向 `Get in touch` 表单，包含姓名、邮箱、联系电话（可选）、主题与留言；旧的大标题、`Join us` 与 `Open News Wall` 按钮已移除。
- 联系表单提交会创建支持请求，并向指定的业务收件箱发送通知；表单成功信息不会暴露实现细节。
- 第二页右侧保留 News Wall、Surveys 与 Community 的 Atlas 视觉入口；Community 已改为可进入的入口，已登录成员前往 `/community`，访客引导注册。
- Atlas 圆形图中央新增 `Let’s begin:` 打字机提示，循环显示阅读新闻、参与问卷与分享观点。

### 公开导航与视觉

- 所有公开页面使用统一的顶部导航、页脚、字体与偏白中性色调；导航在页面下滑时始终可见。
- 导航下拉菜单支持鼠标停留与点击，避免鼠标离开时过快关闭导致无法点击子项。
- 所有主要按钮使用统一的缓慢颜色过渡；不再使用跳大、突兀缩放等效果。
- 字体层级：展示标题使用高对比度衬线体，正文使用已选定的中号衬线体，导航/表单/功能文字使用无衬线体。
- 未登录时不显示聊天客服；登录后的工作区才显示客服入口。

### 新闻与工作区

- 公开新闻墙：`/news`；新闻详情：`/news/:articleId`。
- 登录用户打开新闻详情时继续使用工作区外壳，不会跳回公开首页，也不会显示公开页的工作区跳转按钮。
- 从新闻详情返回新闻墙时，会恢复到用户刚才点击新闻卡片的页面位置。
- 登录后的工作区已统一为更明亮、偏白的中性色调；认证页仍保留其独立品牌分栏布局。
- 用户端工作区不再显示侧边栏或“小组成员工作区”上下文；管理员仍保留侧边栏，用户端改用轻量顶部导航和连续内容区。
- 用户顶部入口显示为 `Home`。Home 保留本地时间问候、问卷引导和硬币余额，并在余额卡中提供打开钱包的行动入口。
- Home 新增使用真实 News Wall 内容的新闻引导区；参与趋势已移至右上角账户下拉菜单中的 `Dashboard` 独立页面，奖励钱包入口继续位于同一菜单。
- 用户端 Home 现使用更紧凑的统一绿色调，并在引导与新闻区加入细线角标；顶部 `Home` 入口使用房屋图标。
- 右上角账户菜单在鼠标离开后短暂延迟关闭，包含 `Dashboard`、奖励钱包和邀请计划入口；登录后的用户端页面在内容底部展示统一页脚。
- 左下邀请提示现提供明确行动按钮和关闭按钮，尺寸已缩小；用户新闻墙新增关键词搜索框，与地区、内容和主题筛选并列。
- 邀请提示已移除会被行动按钮遮挡的说明文字，照片贴齐卡片底边；问卷引导和公开首页的 Atlas、品牌、全球与奖励区块均采用同一套绿色细线角标。
- Home 问候语后会显示中号展示字体的 `Let’s begin:` 打字机提示，循环呈现浏览新闻、开始问卷与探索社区；账户菜单只保留姓名与角色，减少动态效果偏好会自动降级。
- 用户工作区顶部新增 `Community` 入口，对应 `/community`。首版为纯前端社区引导：使用五张独立主视觉自动轮播，提供前后切换控制、细线角标、紧凑的说明卡与五张选择卡。
- 社区详情卡的实际入口使用右侧圆形箭头；底部五张卡只切换说明内容，不承担跳转，避免未来功能并列时误导用户。
- 社区用户侧命名已调整为 `Latest brief`、`Join an activity`、`Discussion`、`Propose a topic`、`Research review`；不再把机构合作方作为用户功能展示。本地活动仅在实际建立后才应面向用户开放，机构侧的研究合作应在后续独立入口开发。
- 社区轮播的五张主图已压缩为网页尺寸并常驻在页面中，只切换当前可见图层，避免换源时出现图像撕裂或大图解码卡顿；说明文案应保持具体、直接，避免使用抽象行业术语。
- 社区页当前只建立信息架构与视觉入口；没有接入评论、投稿、信号、合作机构或报告的后端数据，也不展示任何虚构的社区统计或研究结论。
- 新闻详情现在提供登录后的 `News Signal`：“Worth researching further?”。它只保存个人的选题优先级信号，不显示票数，不判断新闻真假或对错；同类信号是否发展为讨论、问卷或公开综述仍由编辑决定。
- 在线问卷从新标签页返回时会请求最新机会；已开始的题目先在当前会话隐藏，完成记录会在后端持续排除，避免同一已完成问卷重新出现在问卷墙。
- 注册后的原生画像问卷已补齐三项最小匹配信息：常用语言、关注的研究主题、愿意参与的活动形式；现有国家/地区与年龄信息不重复收集。首次完整填写奖励为 `1,000 Coins`，问卷视觉已统一为网站的偏白、深绿与金色体系。

## 核心文件

- 首页：`src/pages/HomeAtlas.jsx`、`src/pages/HomeAtlas.css`
- 公共导航与页面外壳：`src/components/PublicSiteHeader.jsx`、`src/components/PublicSiteLayout.jsx`
- 第二页后续内容：`src/components/HomeLegacySections.jsx`、`src/components/HomeLegacySections.css`
- 新闻墙与详情：`src/pages/NewsWall.jsx`、`src/pages/NewsArticlePage.jsx`
- 路由分流：`src/App.jsx`
- 用户工作区外壳与仪表板：`src/components/AppLayout.jsx`、`src/pages/Dashboard.jsx`、`src/pages/ActivityDashboard.jsx`、`src/pages/WorkspaceSurfaceTheme.css`
- 社区前端引导：`src/pages/CommunityHub.jsx`、`src/assets/community/`
- 联系表单接口：`src/api/supportApi.js`
- 后端联系通知：相邻后端仓库的 `src/routes/supportRoutes.js`、`src/services/supportService.js`、`src/services/emailService.js`

## 已验证

- 前端 `pnpm build` 通过；仅保留既有的包体体积提示。
- 原生画像问卷 V2 已在本地验证：补充字段可保存，首次完整填写仅发放一次 `1,000 Coins`，既有地区、年龄与后台筛选校验保持通过。
- 后端联系表单的来源校验、服务健康检查与一次真实通知提交均已验证。
- 注册页邮箱验证码：已修复无效前端接口配置导致的发送失败；2026-08-04 已在生产浏览器中验证发送成功，并显示 60 秒重发倒计时。
- 线上公开站点已更新至最新前端资源版本。
- 后端的本阶段提交已经推送并在生产服务中拉取、重启。
- 第十四阶段的客户报价、角色通知与研究者工作台视觉收口已在生产环境发布；前端 `npm run build` 通过，公开站点已确认返回对应的新资源。
- 新闻墙在当天上游暂无可用内容时，已验证会展示最近 48 小时内的已验证内容，并明确标记为 `Latest available`；当天有内容时仍只展示当天内容。

## 仓库状态提醒

- 前端本阶段提交已推送，工作区保持干净。
- 后端存在一批与本阶段无关的本地未提交修改；后续工作不得混入、覆盖或提交这些改动，除非用户明确要求。

## 第十阶段：二级首页入口重构（已开启）

目标是重设计视频 Hero 之后的第二页入口区，让新闻、调查与未来社区入口的层级、文案和转化路径更清晰。

社区方向已获确认，先完成用户工作区的前端引导入口；后续应按照“前端信息架构 → 后端数据与权限 → 内容运营”的顺序推进，不要跨阶段把静态引导页宣传为已开放的互动社区。

### 第十阶段后续：研究社区的定位与落地顺序

**平台定位（内部）**

- 用户侧始终说清楚四件事：有什么可参与、是否适合我、要花多久、完成后得到什么；避免把“研究社区”“成员库”等机构术语直接丢给用户。
- 机构侧以后才提供研究项目、样本匹配与洞察交付。平台的价值是把合适的研究机会与愿意参与的人连接起来，而不是一开始同时做新闻媒体、社交网络、研究公司和活动公司。
- 当前可继续用已有的外部问卷机会维持用户侧供给；自有研究先从范围小、目标明确、已确认奖励来源的项目开始，不先承诺无法兑现的活动或奖励。

**画像与选择性开放**

- 注册画像现在只补齐地区/语言、年龄段、关注主题和愿意参与的形式；不再重复收集已有答案。
- 机会应按地区、语言、主题和参与形式筛选后再展示。用户看不到不适合自己的项目是正常产品行为，不应把所有项目都展示成“可参加”。
- 城市、详细住址及其他敏感信息只在某个具体研究确有资格筛选需要时，再单独说明用途并征得同意。

**五个社区入口的真实含义**

1. `最新简讯`：编辑发布的项目动态、已公开的结论或平台公告；不是自动堆砌的新闻流。
2. `参与活动`：展示可报名的在线问卷、访谈、小组或线下活动，必须写明主题、形式、预计时长、奖励、地区/设备要求和截止时间。
3. `讨论`：围绕一条由团队发起、有人维护的问题发表评论；先做受控讨论，不急着开放无限制评论区。
4. `提出一个主题`：用户提交想研究的问题或本地观察，进入审核队列；它是选题来源，不承诺一定立项，也不是对外募资入口。
5. `研究综述`：只发布可公开的匿名汇总、方法说明和结果摘要；项目方专属结果与公开内容必须分开。

**新闻墙与公开结果**

- 新闻墙中的 `News Signal` 只问“这件事是否值得进一步研究”，回答是一个选题优先级信号，不是判断新闻真伪或对错。
- 默认仍优先展示 UTC 当天的新闻；若上游当日没有可用条目，则改为显示最近 48 小时的已验证新闻，并在界面明确标为 `Latest available`，不能将旧内容伪装为当天发布。
- 当前详情页只提供“Worth researching”这一正向信号；保存后不公开票数或生成结论，保留给编辑进行后续主题归并。
- 当同类信号积累后，编辑可将其归入主题，决定是否发起讨论、短问卷或公开综述；没有足够信号时不制造“研究结论”。
- 每个项目的输出必须有可见性：`公开`、`仅项目方` 或 `仅进度通知`。对外发布前确认已匿名化且获得发布许可。

**下一阶段的最小后端路线（按顺序）**

1. **机会匹配与报名（下一步）**：建立“研究机会”数据、资格条件和用户报名状态。用户只看见与自己画像匹配的机会，报名后显示“已申请 / 已入选 / 未入选 / 已完成”。
2. **活动运营基础**：为机会补齐奖励、截止时间、参与方式、名额和通知状态；先支持人工创建和人工审核，不急着自动化复杂调度。
3. **最新简讯与公开综述**：建立可编辑的内容条目和可见性控制，再把真实的项目进度与已获准公开的结果呈现出来。
4. **受控讨论与主题投稿**：先做审核、状态反馈和举报/隐藏能力，再考虑奖励、公开评论或自动发布。
5. **机构侧工作台（最后）**：当机会发布、匹配、报名、完成和结果交付已经跑通并可重复后，再建设机构账户、项目创建、样本筛选和报告交付。

**日常运营闭环（大白话）**

1. 找到一个真实需要回答的问题，并确认谁负责项目、奖励和结果范围。
2. 写清楚招募卡片，让合适的人报名；不合适的人不打扰。
3. 审核报名、确认参与、记录完成与奖励状态。
4. 向参与者说明结果进度；能公开的做成简讯或综述，不能公开的只发项目进度。
5. 复盘哪些主题、地区和参与形式有人响应，用这些信息改进下一次招募。

外部社群可作为公告、答疑和召回渠道，但不能替代站内同意、资格、报名、奖励和研究记录。所有用户文案保持具体，优先使用“报名活动”“分享看法”“查看进度”“阅读公开结果”等直白说法。

### 第十一阶段：在线调查与 Research 入口（已完成）

- `Online surveys` 位于 `/partners#surveys`；标题旁保留 `More surveys` 入口，用于打开已接入的额外问卷墙。已展示的所有匹配问卷在宽屏固定为四列，前端和后端均不再以三条首屏数量截断；当前只请求并展示已获准的问卷来源。通用 CPA 目录在建立人工白名单和完成入账验证前不得自动展示给用户。
- `Research and activities` 现在是独立的 `/research` 页面，用于平台自己的问卷、研究和后续真实活动；当前没有虚构机会，只显示原生“完善参与资料”任务卡。
- 画像任务卡复用 `/panel-profile/horizon-oil.jpg`，采用独立的纵向卡片布局。未完成时可直接继续填写并明确显示首次完成奖励；完成后主按钮显示 `Completed`。卡片下方明确说明：发现匹配的调查、研究或活动时会第一时间通知用户。
- 社区的 `Join an activity` 入口只保留在右侧详情标签中的完整行动按钮；底部五张卡全部只用于选择对应功能，避免未来五项功能都开放后发生误跳转。其余尚未接入数据的社区入口继续明确显示为稍后开放，不伪装成可操作功能。
- 这一流程中的行动按钮统一采用首页主按钮的森林绿胶囊样式：默认森林绿，悬停时以长缓动注入为墨黑；不使用跳大或快速闪烁。

### 第十二阶段：机会匹配与报名（本地实现完成，待部署）

目标是在不伪造活动供给的前提下，建立平台自有研究机会的最小闭环：后台可人工创建机会，用户只看到符合画像的机会，并能清楚查看自己的报名进度。

- 机会的数据至少包含标题、简介、研究形式、主题、参与地区/语言、预计时长、奖励说明、截止时间、名额和发布状态。奖励只能写入已经确认能兑现的项目；没有真实机会时不展示占位活动。
- 匹配规则第一版仅使用现有的地区、语言、关注主题与可参与形式；任何敏感筛选条件均留到具体研究的独立资格问卷中，不扩展注册画像。
- 用户侧的 `Research and activities` 页面展示匹配机会及状态：`Open`、`Applied`、`Selected`、`Not selected`、`Completed`。用户只能对开放且匹配的机会提交一次报名；报名不等同于入选。
- 已新增 `ResearchOpportunity` 与 `ResearchApplication` 数据，并提供可重复执行的 SQLite 建表脚本 `scripts/apply-research-opportunity-schema.js`；不写入任何演示或虚构机会。
- 用户端 `/research` 已从静态提示改为真实匹配墙：仅显示开放、未过期且符合画像的机会；同一机会只能报名一次，成功后立即显示 `Applied`。已有报名会继续展示其 `Selected`、`Not selected` 或 `Completed` 状态，即使项目后来关闭。
- 匹配只使用注册画像已有的国家、语言、研究主题与可参与形式；未完成画像的用户只会看到自己的既有报名状态，并被引导先完善资料。
- 管理员现在可在 `/admin/research-opportunities` 创建真实机会、维护开放状态、查看报名列表并人工更新报名状态。创建表单要求写明项目格式、奖励与摘要；没有确认奖励的项目不应创建或开放。
- 本阶段不承诺自动筛选、自动奖励或自动通知。状态变化的通知和奖励入账将在机会与报名链路稳定后补齐；具体资格追问和活动执行流程仍属于后续小步迭代。
- 已在本地验证：草稿不对用户显示，四项画像匹配生效，重复报名被幂等处理，管理员状态更新能回到用户机会状态。

### Cookie 偏好与营销同意（本地实现完成，待随下次前端一并部署）

- 首页底部新增与现有森林绿、偏白视觉一致的 Cookie 同意横幅。用户可选择接受全部、仅必要 Cookie，或在设置内分别选择分析与个性化广告。
- 选择仅保存在浏览器本地；作出选择后，首页仍保留低调的 `Cookie settings` 入口供随时修改。未选择前，可选类别不会启用。
- 当前未接入可选分析或广告服务，因此不会加载广告脚本或发送广告数据。后续接入时必须仅在对应偏好已同意后加载，并同步更新隐私与 Cookie 说明。

### 第十三阶段：Business 研究入口与项目工作台（已完成并部署）

- `Join us` 现为明确双入口：`Join as a participant` 保持现有故事化参与体验；`Run research with us` 面向企业、研究室/高校、公益或公共机构及独立研究者。参与者不被称为流量、受众库存或可交易数据。
- `/business` 现为稳定跳转入口：新客户直达简洁咨询页，已有 Business 客户直达工作台；不再重复展示一页介绍内容。顶部与页脚均提供一致的机构入口，服务表述统一为定制问卷与定制调研（访谈、可用性测试或小组讨论），不承诺自助发布、名单上传、公开 API 或自动研究交付。
- 新增简洁的 `/business/access` 联系入口，仅收集研究内容、联系邮箱、姓名、机构类型与地区；提交进入人工跟进队列。现有客户可从独立 `/business/login` 登录既有工作台，不公开具体业务收件地址。
- 工作台以“定制问卷 / 定制调研”两个明确入口开始，已有项目提供状态筛选；请求可真实创建并保存，状态从 `Received` 开始。内容记录研究目标、目标人群、方式、地区/语言、人数、时长、时间安排与奖励预算。
- 参与者端仍只展示主题、资格、方式、时长、奖励、设备要求与申请状态。客户名称和项目商业细节默认不对参与者公开，只有客户明确授权才可展示。
- 首页视频和 Join 选择页不叠加装饰线稿；Business 页只使用项目简报语义的原创图形，避免干扰主内容。
- `scripts/apply-business-workspace-schema.js` 可重复执行，并会补齐 Business 联系资料的地区列；前端构建与后端 Business 工作台验证已通过。
- 对外文案不得披露项目筛选、是否承接、内部审核、供应商、定价或履约判断；这些只属于内部运营。问卷产品的功能地基与公开边界见 `docs/SURVEY_PRODUCT_FOUNDATION.md`。

### 插画系统：首轮上线与性能优化（前端实现完成）

- 首页视频首屏继续保持干净；视频后的 Atlas 入口区在 News Wall、Surveys、Community 之外新增第四张 `Business` 悬浮卡，直接进入 `/business`。
- `Business` 卡采用握手线稿；Business 首屏保持项目简报卡与纸张层次的清晰阅读，"研究反馈协作"线稿移至后续内容区，并以图文并置的编辑版式融入页面。
- 参与者的 Research 空状态采用清单人物插画；只有在没有匹配机会时显示，不会覆盖真实机会内容。
- 插画从高分辨率源图裁制为匹配实际显示尺寸的压缩 JPEG：首屏不再请求大幅插画，非首屏插画使用延迟解码与懒加载；避免为装饰而加载过大的 PNG。

### 第十四阶段：自助问卷基础与视觉收口（已完成并发布）

**问卷产品基础**

- Business 客户可创建自助问卷并在工作台中编辑、发布、查看答卷；标准题型当前覆盖单选、多选、短文本、长文本与评分。
- 生命周期固定为 `Draft → Pending review → Published → Archived`。有真实答卷的已发布问卷锁定题目结构；需要改题时复制为新草稿，保证历史数据一致。
- 公开问卷带基础反滥用和重复提交拦截，并对异常短填写时间作可疑标记；涉及特殊类别信息时，在发布前要求创建者确认其合规责任。
- 公开答题页会说明问卷创建方和基本数据用途；答卷归创建者所有，平台提供技术服务。公开文案不得披露内部审核、风险阈值或运营判断。
- 管理端保留问卷发布审核与答卷查看能力；未通过发布前不公开链接。没有展示虚构样本库、自动研究承诺或未开放的高级分析能力。

**本轮视觉基线（后续页面必须遵守）**

- 默认英文。展示衬线体只用于简短的主标题、报告式小节标题；导航、正文、表单、字段标签、状态和工作台全部使用无衬线体，不能把辅助信息放大成展示标题。
- 研究者端与参与者端共用偏白纸张底色、深森林绿、低饱和浅绿和细边框；工作台保持功能导向，不使用大面积营销式大字。
- 参考 `Keerti_IP Positioning.pdf` 的版式：内容有明确阅读列、正常字号与行高、足够留白。文字放不下时扩展布局或留白，不能压缩字段、折断电话选择器或靠满屏装饰填空。
- 首页联系区已改为可读的两栏字段布局：姓名/邮箱、地区/主题并列，电话号码独占整行。国家选择完整显示旗帜、国家名称与区号；移动端国家和号码各占一行，避免任何选择器或输入框被压成窄缝。输入控件强制使用无衬线字体。Business 询价页同样遵循这一规则。
- Business 咨询、登录/注册与工作台的标题尺寸已收敛；衬线只保留在短标题，表单和说明沿用统一无衬线层级。Business 登录/注册的条款文字使用正常行内排版，避免链接被 flex 布局拆散。后续新增插画应放在预留留白中，而非替代内容或遮挡字段。
- 参与者端 `Research and activities` 的任务卡、机会标题和小节标题已改为功能性无衬线层级；保留纸张底色、深绿、浅绿和细边框，不将工作台卡片做成营销海报。
- 研究者端的咨询、登录与注册页现在只显示 `Back`，目标为 `/business`；已登录研究者会由该入口回到自己的 Business 工作台，不会再被带到公开首页。
- 管理员工作区现在只展示运营入口：Client projects、问卷审核、销售与支持、研究机会、参与者资料、奖励、问卷来源与系统运维。成员端的首页、新闻、社区、问卷墙、钱包和个人资料不再出现在管理员导航中；管理员直接访问这些成员路径会回到后台总览。公开网站不显示任何管理员入口或文案。
- 研究者工作台创建的项目会进入管理员的 `Client projects` 队列，管理员可查看项目概要并更新项目状态；其中的问卷继续进入独立的 `Questionnaire review` 队列。该审核页可查看待审、已发布、已归档或全部问卷。销售咨询标记为 `Business sales`，同时进入管理员的 `Sales & support` 队列，并向受环境变量 `CONTACT_FORM_INBOX` 管理的联系邮箱发出通知；具体邮箱不写入前端、文档或公开页面。

**本轮变更与验证**

- 变更文件：`src/pages/HomeAtlas.jsx`、`src/pages/HomeAtlas.css`、`src/pages/BusinessAccess.jsx`、`src/pages/Business.css`、`src/pages/WorkspaceSurfaceTheme.css`。
- 本轮后台收口还涉及前端的 `src/App.jsx`、`src/components/AppLayout.jsx`、`src/pages/AdminBusinessProjects.jsx`、`src/pages/AdminBusinessQuestionnaires.jsx`、`src/pages/AdminSupportTickets.jsx`、`src/api/realApi.js`，以及相邻后端仓库的 Business 项目管理、支持路由与邮件服务。
- 已执行 `npm run build`，生产构建通过；仅保留既有的包体积提示。已在本地桌面与 390px 移动端复核：首页和 Business 询价页电话控件无水平溢出；Business 登录/注册文案与字段正常换行。公开首页与 Business 返回入口已做浏览器核验；后端问卷/支持校验和修改文件语法检查通过。本地验证环境未配置邮件投递凭据，因此没有发送测试邮件。
- 本轮视觉收口已随前端发布上线。后续仍应在桌面与移动端浏览器检查后再合并新的视觉改动。

### 第十四阶段：客户报价、启动闸门与角色通知（已完成并发布）

- Business 项目现在按正式商业阶段流转：`Draft → Submitted for review → Quote required → Quote sent → Client accepted → Funded → Recruiting → Live → Completed`。阶段不是对外营销承诺，而是运营后台与客户工作台共享的真实项目进度。
- 客户可免费建立项目与问卷草稿，不需要管理员批准“创建”。非问卷研究简报由客户提交进入 `Submitted for review`；样本招募或研究支持问卷也会先进入该状态。管理员确认范围后将项目置为 `Quote required`，再发送正式报价。
- 管理员在 `Client projects` 中录入币种、金额、范围、有效期和可选条款。发送报价会创建可版本化记录，并把项目置为 `Quote sent`；新报价会替代尚未回应的旧报价，避免客户按过期版本确认。
- 客户通过工作台的报价弹窗接受或拒绝；报价邮件只作为回到已登录工作台的提醒入口。接受会留存账户、报价版本和时间，并使项目进入 `Client accepted`。当前不接入线上收款；管理员只应在款项确认后手动设为 `Funded`，再进入招募与上线。因此“接受报价”绝不表示“已付款”。
- 已新增两端不同的站内通知铃铛。客户端接收报价、资金确认、招募、上线与完成通知；管理员接收新项目、项目提交、问卷审核、报价接受或拒绝等运营通知。报价发送同时尝试发送邮件提醒；本地环境未配置投递凭据时，不影响站内通知或项目状态。
- 问卷内容审核与商业阶段严格分离：自行分发问卷审核通过即发布；需要平台样本或研究支持的问卷审核后只进入 `Content approved · awaiting launch`，不收集答卷。仅当项目已进入 `Live` 时，后台才会开启已批准问卷的收集。未通过内容审核的问卷不能被项目状态直接上线。
- 新增 `BusinessProjectQuote` 与 `Notification` 数据模型、迁移文件和可重复执行的本地 SQLite 建表脚本 `npm run schema:business-quotes`。已有本地数据库未写入 Prisma 迁移历史时，应使用该幂等脚本，不要强行运行会要求基线的迁移命令。
- 新增验证 `npm run verify:business-commercial-workflow`，覆盖报价、客户接受、资金闸门、内容审核和受控上线。前端 `npm run build`、Business 工作台验证、问卷验证、商业流程验证与支持知识库验证均通过；仅保留既有包体积提示。
- 前后端已提交并发布。生产数据库已补齐 Business 工作台所需的兼容字段；后续发布仍应先执行对应的幂等建表/补列脚本，再切换应用版本。
- 研究者工作台在发布后继续做了视觉收口：短标题使用展示衬线，其余工作台文字、字段与操作均使用无衬线；左侧重复的联系销售入口已移除；通知在研究者端以居中独立浮层显示；头像菜单可进入 Account；账户页面不再展示登录 IP 或登录地区。Community 仍是登录后可查看的前端预览，尚未开放为公共互动功能。
- 下一步若接入真实支付，应以受控支付页和付款回调驱动 `Funded`，不能把前端按钮或邮件回复当作付款成功；接入前还需确认账单、税务与退款规则。

### 第十五阶段：功能完善与插图加入（已开启）

**目标**

- 把已上线流程的关键空状态、加载/失败反馈、返回路径与通知落点补齐，使参与者、研究者与运营端的现有功能可连续完成，而不是只展示静态界面。
- 在不挤压字段、不遮挡操作、不制造虚假供给的前提下，为首页、参与者研究入口、研究者工作台和后续确认页加入用户提供或明确授权的插图。

**功能工作边界**

1. 优先补齐真实已有数据链路：研究者项目创建、问卷编辑与审核、报价阅读/接受、资金确认、项目上线、参与者机会报名、通知跳转及账号资料入口。
2. 每个空状态都必须给出当前可执行的下一步；失败反馈说明用户可如何重试，但不暴露内部系统、审核规则或运营判断。
3. Community 在具备内容、审核、举报、可见性与用户同意机制前，继续作为信息架构预览；不得提前公开评论、投稿或虚构互动数据。
4. 管理端继续只作为内部运营工作台，不在公开站点、参与者端或研究者端增加管理员入口或描述。

**插图工作边界**

1. 先接入用户后续提供的素材；没有素材时仅保留已有留白和简单界面图形，不凭空制造人物、研究结果或活动现场。
2. 每张插图必须服务于一个具体任务或空状态，并提供有意义的替代文本；纯装饰图使用空替代文本。
3. 新图按实际展示尺寸导出并压缩，首屏优先级最高的资源才可预加载；其余使用懒加载和异步解码。不得以大图、自动播放或复杂动效替代内容层级。
4. 插图不改变表单、国家/电话控件或可点击区域的最小可用宽度；桌面与移动端均需复核。

**本轮：客户研究条款与联系入口收口（前端完成，待随下一次统一发布）**

- `/business/access` 的联系表单已与左侧主视觉顶齐，移除右侧上方无意义留白；移动端继续保持单列阅读与输入顺序。
- 新增公开的 `/business/terms`：适用于使用客户工作区的机构和独立研究者，明确账户授权、研究简报和问卷责任、受访者告知与合法基础、特殊信息和未成年人保护、报价/确认/启动条件、受约定的招募支持、项目数据和保密、服务可用性与联系渠道。文本只描述已提供的产品流程，不承诺样本量、工期、研究结论、付款方式或未上线能力。
- Business 注册和登录页的条款链接现在指向 `Business Researcher Terms`；参与者注册仍使用通用 `Terms of Service`。`/terms` 同时说明客户工作区还需遵守该专属条款。
- 所有使用 `LegalPageLayout` 的公开条款与隐私政策统一标注 `Effective July 5, 2026`。条款为产品版草案；在面向特定司法辖区或签署大型项目之前，仍应由适用地区的合格法律顾问复核。
- 已执行 `npm run build`，生产构建通过；仅保留既有包体积提示。

**本阶段验证清单**

- 每次功能改动至少验证对应角色的成功、空状态和失败/重试路径。
- 每次插图接入均检查桌面与窄屏裁切、对比度、键盘焦点、图片加载行为和包体变化。
- 发布前执行前端 `npm run build`；后端改动同时执行对应语法检查与最小流程验证。只有需要结构变更时才运行幂等数据库脚本。

### 第十五阶段：研究者工作台服务化收口（进行中）

- Business 工作台的默认入口已从“让客户自行搭建问卷”调整为“保存研究委托简报”。客户可请求问卷设计或托管研究；前者由团队根据研究目标交付问卷，后者再按约定范围处理研究执行与参与者招募。不要将 AI 自助问卷编辑器作为新的公开卖点或继续扩张其题型能力。
- 既有自助问卷功能保留给已有项目和受控例外，不作为新项目的默认路径。新建的研究委托不会自动创建自助问卷；客户先保存简报，再在准备好时提交以获取方案。
- Projects 新增真实的草稿查看、编辑和删除操作；只有尚未提交且未产生答卷的草稿可以删除，已进入流程的项目不会被前端菜单伪装成可修改。
- 左侧第二入口命名为 `Research support`，用于展示账户、项目概况和准备研究简报的真实下一步；它不是尚未实现的团队协作、共享权限或样本库。若未来开发多人协作，需先建立账户成员、项目权限、邀请、撤销和审计闭环，再公开入口。
- Business Account 已从成员端 `/profile` 分离为 `/business/account`，使用工作台一致的无衬线信息层级，右上角返回 Projects；退出只会前往 Business 登录页。

### 第十五阶段：插图与入口层级（本地完成，待与后续更新一并发布）

- 已接入四张用户提供的原创插图：Business 公开页使用道路场景作为主视觉；Research support 使用校园场景替换旧的握手图；Business 注册与登录页分别使用独立的工作场景插图。插图保留在页面结构内，不以贴图式深色卡片覆盖内容。
- 公开 Business 页现在清楚说明服务型研究入口；Research support 的行动为 `Create a questionnaire`，Projects 页的主行动为 `Contact sales`。两页不再错误地使用同一个“创建问卷”入口。
- Research support、Projects 与项目卡片已统一到白色页面、低饱和浅绿卡片、功能性无衬线中号文字的工作台层级；展示衬线不用于项目状态、说明或操作文字。
- 登录和注册插图均是装饰性背景，使用空替代文本；其余插图保留与具体页面语义相符的替代文本。非首屏图片使用异步解码和延迟加载。
- 已执行 `npm run build` 与 `git diff --check`，前端构建通过；生产部署按当前安排暂缓，发布前仍需完成桌面与窄屏的浏览器视觉复核。

### 第十五阶段：插图版式修正（本地完成，待发布）

- 根据桌面截图复核，Business 登录/注册原先的图片分栏会在长表单旁显得像独立海报，已改为固定视口高度的背景图分栏：注册使用城市插图，登录使用公园插图；表单内容可独立向下阅读，插图本身不随表单高度拉伸。
- Business 公开页恢复纯白页面底色；服务卡保留低饱和浅绿。首屏与服务区之间的无效留白已收紧，主视觉高度改为受控比例，避免形成悬浮小卡片和大段空白。
- Business 问卷编辑器的题目卡、题目文本、控件和侧栏均强制使用功能性无衬线层级；题目卡改为浅绿色，与研究者工作台项目卡保持一致。
- 本轮执行 `npm run build` 与 `git diff --check` 均通过。当前会话无法连接本地浏览器控制器，因此上线前仍须在桌面和窄屏浏览器核对背景裁切、长表单滚动与焦点样式。

### 第十五阶段：认证分栏与字体二次修正（本地完成，待发布）

- 登录与注册继续使用两张不同插图（登录为公园、注册为城市），并移除了原先洗淡画面的白色渐层。两页采用无间隙的左右等宽分栏，插图和认证区各自占满一半页面；认证区不再受旧的窄面板宽度限制。
- Projects 使用展示衬线标题；项目卡标题、Research support 小标题、问卷编辑器标题与题目文本使用阅读衬线；状态、说明、筛选、输入和操作继续使用无衬线。这样保留品牌字体层级，同时不牺牲功能界面的可读性。
- 已再次执行 `npm run build` 与 `git diff --check`，均通过。

### 第十五阶段：联系入口与统一页脚（本地完成，待发布）

- Business 注册页认证区已移除顶部内边距，标签与左侧插图顶端对齐；长表单仍可向下阅读，不会拉伸插图。
- Support Chat 现在只向参与者账户显示。Business 客户和运营角色不再看到以参与问卷为中心的聊天引导。
- 联系销售页采用用户提供的城市插图，并沿用与认证页一致的左右分栏和窄屏收束行为。
- Business 访问页补齐与公开站点一致的深绿页脚；页脚社交区新增 WhatsApp 社群小图标，链接为用户提供的公开频道，标题/辅助名称为 `Join our community`。
- 已执行 `npm run build` 与 `git diff --check`，均通过。

### 第十六阶段：全站语言一致性（进行中；下一阶段重点）

**目标**

- 用户在任意 GuanyiSearch 页面选择一种语言后，应跳转到同一页面的 `?lang=<语言代码>` 地址；刷新、页头、页脚、同站内跳转与登录后返回路径都必须保留该语言。
- 语言选择器提供 18 个既定语言；使用简单的网格地球图标，不新增难以理解的图形。页头和页脚都提供相同的切换能力。
- 不将“首页标题已切换”误表述为全站本地化。每个页面需逐项验收可见正文、表单标签、状态、错误反馈、空状态、导航、无障碍标签及页内链接。

**本周新增重点：研究端的跨市场研究入口（已开始）**

- 用户已确认研究端仍以 `定制问卷` 和 `定制研究` 为两条服务入口；不将其改为通用自助问卷工具或自动化研究承诺。
- 研究端首页现以两项服务为主入口，而非项目列表：`定制问卷` 是围绕明确决策和目标人群建立、编辑并收答的私有问卷；`定制研究` 用于访谈、可用性测试、焦点讨论或需专门招募与研究设计的跨市场议题。真实流程为“简报 → 团队补充讨论 → 方案与报价 → 确认后执行”；不得跳过人工范围确认。
- 结果拥有独立工作区入口。仅当项目实际拥有问卷时，结果入口才列出该项目；所有汇总和逐份答卷均来自真实已收回答，绝不展示示例报告或示例百分比。
- 后续 AI 路线仅作内部规划：先从自然语言研究意图生成**可编辑**的简报/问卷草稿，再由人工确认研究范围与报价；在有足够真实、可追溯答卷及明确质量规则后，才考虑将 AI 用于开放题主题整理、分组切片和带依据的报告草稿。AI 不可替代样本设计、跨市场可比性判断、质量控制或研究结论的人工审核，也不得在功能上线前对外宣称可用。
- Business 客户首次进入工作台时，先完成两步简短了解：`独立研究者 / 组织`，以及 `探索研究问题 / 了解当地市场 / 准备市场决策`。第二题采用客户可理解的市场研究表述，而非“研究爱好者”等模糊标签。选择只用于让工作台呈现适合的起点，不构成自动资格、样本匹配或服务承诺。
- 注册页不再要求选择国家/地区或机构类别；跨地区研究的市场与语言在具体项目简报中说明。首次进入工作台改为暖白的独立过渡页：先选 `个人 / 组织`，再选研究起点；选择“组织”时增加第三步，按 `企业或商业机构 / 研究或教育机构 / 非营利或公共机构` 保存机构类别。
- 为兼容尚未部署新引导接口的服务端，前端在该接口缺失或暂不可用时会把上述非敏感偏好暂存于当前设备并放行进入工作台；接口可用时仍优先写入账户。该降级不保存项目、问卷、报价或答卷，也不替代正式账户数据。
- 项目简报现按“决策与对象 / 方法与实际范围”组织：除研究目的和对象外，客户可保存目标市场或社群、相关语言、预计样本量、时长、时间节点和参与者激励状态，并在已保存简报中复核。以上均为规划信息；保存不会启动研究、招募参与者或承诺报价、范围与交付日期。
- 引导结果保存于 BusinessProfile 的 `researchRole`、`researchIntent`、`researchOnboardedAt`。生产发布前先运行后端 `npm run schema:business-workspace`；对应 Prisma 迁移为 `20260918003000_add_business_research_onboarding`。
- 研究者工作台的问卷项目新增 `View results`，进入 `/business/projects/:projectId/results`。该页仅从真实已收答卷计算选择题分布并显示项目所属的逐份回答；零答卷时显示明确空状态，绝不放入示例百分比、示例图表或虚构结论。被系统标记为异常过快完成的答卷提示人工复核，默认不参与汇总百分比。
- 结果页不等于高级分析或正式研究交付：导出、跨市场对比、自动解读、样本代表性判断、加权和可公开报告仍未开发，不得提前对外表述为已可用。
- 新增工作台页面已使用同一语言状态维护 `lang` 参数的结果页跳转；完整可见文案的 18 语言资源、真实登录态多语言验收，仍属于第十六阶段未完成项。

**本阶段确认的平台定位与文案入库节奏（2026-09-16）**

- 平台对外的核心定位是跨市场的消费者洞察协作网络：连接愿意参与的人、研究发起方与组织，让经同意、审慎收集的本地声音帮助商业、文化、日常生活及公共议题形成更清晰的决策。它不是同时承诺新闻媒体、开放社交网络、研究公司和活动公司的集合体。
- 公开的跨市场议题用于持续了解语境；当组织需要为明确决策取得更贴近市场的答案时，可委托范围聚焦的本地研究。用户侧仍须优先说明可参与内容、适配性、预计时长和完成后的回报；不得将尚未落地的社区互动、投稿、评论或奖励机制表述为可用功能。
- 首页 `CommunityHub` 的两张轮播卡现以可购买的市场进入问题表达优势：先说明在新市场进入前验证当地消费者、渠道和产品表达，再说明平台以本地消费者语境、渠道/市场信号支持更清晰的 go-to-market 判断。不得将企业内部跨区域团队协作、时区交接、管理冲突或组织诊断写成平台主营服务；那属于不同的客户问题与预算。
- `CommunityHub` 当前的两张卡片仅用于说明上述两条研究路径和引导研究委托，不代表已开放新的社区产品。研究拼图采用用户提供的叠图：以原图约 21% 高度的横向分界点作为下方照片框上缘，照片框延展至原图下缘之外少许；人物叠层保留在完整画幅上方。画幅已同步放大，底图与叠层均按同一原图比例裁切，避免出现中段小卡片或两次裁切未接合的问题。
- 这轮新增或调整的前端可见文案暂不单独写入 `LanguageContext`；待第十六阶段当前页面更新收口后，统一以同一语言资源、同一键名和同一验收矩阵录入。收口前这些英文文案不得被标记为已完成全语言本地化。
- 首页视觉收口：跨市场研究卡现为轮换首屏；卡片保留原有内缩、边角装饰和尺寸，两侧的空白区域改为同色绿色填充，不以压缩卡片解决缝隙。首页主视觉与该卡之间保留更充足的垂直留白；主视觉裁切使用更明显的连续圆角。研究视觉直接显示用户提供的透明 PNG 原图，不再用两层元素重裁切或添加下层投影。首页页面过渡容器不再应用会捕获固定定位的变换，因此公共页顶栏可持续固定于滚动视口上方；窄屏下页面内容保留安全内边距并限制研究卡文案与图片在容器内换行。

**已完成并已本地验收**

- 语言状态的唯一来源为 `src/components/LanguageContext.jsx`：URL `lang` 参数优先，其次为本地保存的选择；选择新语言会用完整页面跳转重新加载当前地址。
- 公开页头、公开页脚及其同站内链接已保留语言参数；页脚的标签本身也会随已支持的语言切换。
- 首页及已收口的公开内容页、参与者登录/注册面板、找回密码页、隐私政策和通用服务条款已接入语言状态。
- 隐私政策和通用服务条款目前有完整的简体中文与繁体中文正文、目录和章节编号；繁中已通过浏览器直达验收。找回密码页亦已通过繁中直达验收。
- 用户端工作区外壳已开始保留语言参数：顶部主导航、退出后登录、个人资料入口及管理员导航都不会丢失当前语言。
- 参与者 `Home`（`/dashboard`）现已接入同一语言状态：问候语、打字机提示、余额说明、问卷引导、奖励路径、新闻引导、空状态和相关无障碍标签均不再自行保存语言；钱包、问卷、资讯和资讯详情跳转会保留 `lang`。简体与繁体中文已写入本轮完整文案，其他既定语言继续明确回退英文，不能将其视作完成翻译。
- 本轮执行 `npm run build` 与 `git diff --check` 均通过。构建仅有既有的前端包体积提示。

**明确未完成项（下一阶段必须继续，而非对外宣称已完成）**

1. 登录后参与者页面：`ActivityDashboard`、`CommunityHub`、`ResearchActivities`、`SurveyPartners`、`SurveyList`、`Wallet`、`PanelProfilePage`、`Profile`、`Settings`、新闻墙与新闻详情，需要逐页将文字、状态、表单和错误提示接入同一翻译源；`Dashboard` 已完成语言状态、跳转参数以及简繁中文文案接入，其他 16 种语言的正文仍待翻译。
2. 认证余项：`ResetPassword`、`BusinessAccess`、Business 工作区和公开问卷需要逐页补齐所有 18 种语言的可见文案与跳转参数。不得把服务端原始错误、第三方登录组件文案或未翻译的浏览器提示伪装成已本地化。
3. 法律页面：简体和繁体中文已写入前端；其余语言仍显示英文正文。在任何法律文本作为正式适用文本前，应由适用法域的合格法律顾问审阅，不应使用机器翻译或首页文案替代法律内容。
4. 所有新页面应先从 `useLanguage()` 读取语言和文本；所有站内 `Link`、`navigate`、重定向和登录回跳都用 `withLanguage(path, language)` 保留参数。不得复制一份相互独立的语言状态。

**2026-09-17 续接顺序**

1. 先完善第十六阶段现有页面的语言资源与语言参数传递，不新增社区、活动或研究交付功能。
2. 按“一个页面/一条真实流程”推进：先补齐可见文案、表单状态、错误反馈和无障碍标签，再验证站内跳转不丢失 `lang`。
3. 每完成一页，按英文、简体中文、繁体中文、日文及一种拉丁字母语言做最小验收；尚未有完整译文的语言要明确回退英文，不可标记为已本地化。
4. 首页第二屏的两张跨市场研究说明卡已完成本轮视觉收口：第二张使用用户提供的合成素材，人物上半部对齐素材的原始横向接线，下方圆角照片框向下延展并裁至框底；不添加白色描边或额外拼接。除非用户再次提出视觉调整，此区域不再作为第十六阶段的工作重点。
5. 本周优先完成研究端已确认的真实流程：首次研究意图引导、定制问卷/定制研究简报、问卷结果空状态与真实答卷预览；每一项先完成权限、持久化、成功/失败状态和语言参数，再扩展下一项。

**本阶段验收矩阵**

- 每新增一种页面类型，至少检查英文、简体中文、繁体中文、日文和一种拉丁字母语言；检查 URL、页头、正文、页脚、表单标签、成功/失败状态以及内部链接。
- 在语言选择器中选中任一语言时，确认浏览器地址仍为同一路径且 `lang` 更新；刷新后选择不丢失。
- 对登录后页面使用真实的角色会话验证，不以未登录跳转页代替。对没有可用测试会话的角色，至少完成代码层的语言参数传递检查，并在交接中标记为待验收。
- 不把翻译文案、问卷回答、错误日志或帐户资料发送给第三方翻译服务；翻译资源必须随前端代码受版本控制。
- 2026-09-15 本地已提交第十六阶段 `Dashboard` 改动（`0c20a1b`），并通过 `npm run build` 与 `git diff --check`。该会话无法推送 `main` 或直连正式发布：当前本机 GitHub 写入授权和 Vercel 登录令牌均不可用；恢复凭据后可从该提交发布，再验收英文、简中、繁中、日文及一种拉丁字母语言的真实登录态页面。

### 第十七阶段：研究端可信交付与语言发布闭环（代码与发布完成；真实账户验收待执行）

**阶段定位**

- 本阶段不是把研究端包装成通用问卷工具，也不是提前上线 AI 研究员。目标是把客户已经看得见的两项服务、项目简报、真实答卷结果、人工范围确认和跨语言使用路径收成一条可理解、可验证的研究流程。
- 对外只销售两项明确服务：`定制问卷` 用于围绕决策与目标对象共同确定问题、形成私有问卷并收集真实答卷；`定制研究` 用于访谈、可用性测试、焦点讨论或需要专门招募/研究设计的议题。两者都经过“保存简报 → 补充讨论 → 方案与报价 → 确认后执行”，不能把保存表单、生成草稿或浏览结果写成研究已经启动。
- 长期 AI 的正确位置是辅助而非替代：客户自然语言描述可生成**可编辑**的项目简报或问卷草稿；范围、样本、跨市场可比性、质量控制、报价和结论仍由人工负责。仅在有足够真实且可追溯的答卷后，才可考虑开放题主题整理、分组查看和带证据/局限说明的报告草稿；这些能力目前均未上线。

**当前事实与入口约定**

- 工作台首页应先帮助客户选择服务，而不是先把项目列表、研究支持页、标签页或尚未兑现的 AI 功能堆在首屏。客户可以从首页创建定制问卷、申请定制研究，或进入“结果”查看自己实际拥有问卷的项目。
- `定制问卷` 新建的是私有项目/问卷草稿，后续问题设计、审核和答卷收集仍受现有真实流程约束；`定制研究` 只保存研究委托简报，不自动创建问卷、更不自动招募。
- 结果入口的唯一可信规则：无问卷则不列项目；零答卷显示空状态；汇总和逐份回答只来自已收的真实数据；异常完成答卷提示复核且默认不进入汇总。不得为演示效果加入假百分比、假图表、假报告或虚构研究结论。
- 首次进入仍采用暖白过渡引导：先选择“个人/组织”，再选择“探索研究问题/了解当地市场/准备市场决策”；组织客户再选择“企业或商业机构/研究或教育机构/非营利或公共机构”。国家、地区与语言不应在注册阶段强制填写，而应在具体项目简报中说明。

**部署核验（2026-09-18）**

- 前端仓库远端默认分支为 `main`；远端 `main` 与本地 `HEAD` 均已确认指向 `97c258d`（`clarify research workspace services`），工作树干净。因此“代码尚未推送”不成立。
- 仓库包含 Vercel 配置，但该配置只定义静态站点安全响应头和 SPA 重写，不能证明生产域名已使用哪一次构建。正式域名仍显示旧页面时，应在托管平台确认项目连接的是此前述仓库的 `main`、最新构建已成功、域名指向该项目，并在无痕窗口核验缓存；不能靠再次提交相同代码解决部署绑定或构建失败。
- 2026-09-18 已直接读取正式域名返回的前端脚本：其中包含 `Research with a clear next step`、`Custom questionnaire`、`Research services` 和问卷草稿成功提示，证明 `97c258d` 的研究端首页代码已进入当前静态生产包。若某个账户仍看不到结果入口或引导，应继续按登录角色、首次引导状态、API 数据和浏览器缓存逐项排查，不应再判断为“前端没有推送”。
- 后端研究引导字段依赖 `npm run schema:business-workspace` 与对应的幂等生产数据库更新；前端对该接口暂不可用已做有限的本机偏好降级。降级只能保证客户能看见工作台，不能证明服务器已拥有最新引导持久化能力。

**第十七阶段验收顺序**

1. 以真实新注册的 Business 会话验证：个人与组织两条引导、组织第三步、刷新后的进入工作台，以及旧服务端时的安全降级；不以截图或本地状态代替。
2. 分别创建定制问卷与定制研究简报，检查项目卡、简报字段、提交/报价闸门及“保存不等于启动”的文案；确认问卷项目才会进入结果索引。
3. 用真实受控答卷验证结果页的零答卷、有效答卷、异常答卷与逐份回答状态；在无数据项目确认不出现任何分析或下载承诺。
4. 按英文、简体中文、繁体中文、日文和一种拉丁字母语言逐页检查研究端：引导、首页、简报、项目、结果、登录回跳、页头/页脚与所有站内链接均保留 `lang`。新增可见文案统一进入 `LanguageContext`，未有译文的语言明确回退英文。
5. 每次正式发布后用生产域名完成一次匿名页面检查和一次真实 Business 会话检查；记录部署提交、构建结果和未通过项，避免再次将 Git 推送与生产发布混为一谈。

**2026-09-19 首轮验收记录**

- 正式站点匿名检查：`/business/login?lang=zh-CN` 的认证面板和页脚链接保留 `lang`；直接使用不在 18 个支持代码中的 `?lang=en` 时，页面回退到已保存的 `en-GB`，因此后续验收使用 `en-US`、`zh-CN`、`zh-Hant` 三个正式代码。
- 本地复现并修复：Business 登录/注册外层的返回、工作区标题与说明未随简繁中文切换；注册面板的 Business 标题、联系人/验证码字段、按钮和条款名称也有英文残留；登录与注册标签切换曾丢失 `lang`。修复提交为 `be8bd51`，`git diff --check` 与 `npm run build` 已通过，尚未发布。
- 真实会话尚未开始：当前浏览器没有可控的 Business 登录会话；未代替用户创建账户、提交注册资料或尝试付款。待用户在保留的 Business 登录页完成登录后，继续执行个人/组织引导、两类简报、结果与报价路径。

### 中国市场洞察能力展示页（已实现，待人工线上检查）

- 新增公开页面 `/china-market-insights`，并同步加入公开桌面导航、移动端导航与页脚的 `For organisations` 栏目；所有站内入口保留当前 `lang` 参数。导航和页脚在简繁中文分别显示“中国市场洞察能力”与“中國市場洞察能力”；其余本页语言资源仍待人工确认后统一录入。
- 页面以首页同源的深绿外框、暖白编辑面板、细线角标和圆角媒体组织内容，不使用满屏营销式大标题。首屏为覆盖式导航与北京城市全屏图；第二屏为深绿执行能力页、三枚浅绿标签，第三屏为压缩留白后的故宫洞察页。已删去的图文屏的“why/语境”说明已并入故宫页，不与能力标签混排。用户提供的北京城市与故宫照片已按实际展示尺寸压缩后随前端资源管理。
- 已确认可对外表述的内容包括：中国一至四线城市研究触达、成熟研究网络和优质受访者社群、多城市/连续性项目、特定细分人群招募、从源头到交付的人工质检及拒绝机器人数据；初步数据 48 小时交付明确限定为“已约定的项目范围支持时”。不将该页写成自动研究、自动报告或无条件时效承诺。
- 本地已完成窄屏视觉与入口检查，`git diff --check` 与 `npm run build` 通过；待正式发布后由用户人工检查英文文案、实际素材观感和商业承诺表述。

**2026-09-19 版式二次收口**

- 首屏已改为北京天际线全屏背景，复用首页透明覆盖、向下滚动后恢复暖白实底的固定导航；深绿侧边画框保留。修复了页面过渡容器捕获固定导航的问题，离开首屏后导航仍固定在视口。
- 移除原先两张难以解释的并列图片及“Context / Everyday movement”标签；第二屏只保留故宫图，并以“中国并非单一消费者故事”的直接文案说明城市、地区和社群差异。
- 执行能力区移动到第三屏；三个能力标题现在以深墨绿色编号标签呈现，而非大号标题。首屏、第二屏与能力区均完成本地视觉检查。

**2026-09-19 收口记录**

- 已将研究端最先到达的三处界面纳入 `LanguageContext`：服务首页、问卷结果索引和首次进入引导。简体/繁体中文覆盖服务定义、真实结果提示、个人/组织与研究意图选择、组织类别、关键账号菜单与结果入口；相应账户、登录和联系销售跳转保留当前 `lang`。
- 问卷结果详情页也已纳入同一资源：加载/错误/空状态、汇总口径、异常答卷提示、逐份答卷提示及日期显示会按语言切换。服务端接口先按当前登录客户的项目所有权读取问卷，再只返回答案、提交时间、耗时和质量标记；不向研究客户返回答卷者的邮箱、网络地址或浏览器标识。完成异常快的答卷会标记为待复核，默认不参与页面百分比汇总。
- 新建服务选择和完整项目简报表单现已纳入同一资源：两种服务的解释、项目名称、决策/对象、市场/语言、方法、预计人数和时长、时间安排、参与者激励、补充材料及“保存不等于启动研究”的边界均有简体/繁体中文。字段值和后端枚举仍保持不变，不用翻译后的显示文案替换真实数据值。
- 项目列表、筛选状态、项目菜单、已保存简报阅读窗、报价审阅/拒绝反馈、成功/失败提示、加载提示和工作区导航标签现已纳入同一资源；简体/繁体中文随 `lang` 切换，日期和货币格式也使用当前语言。客户端不再将服务端原始错误直接显示给客户，避免出现跨语言错误或暴露不必要的实现信息。
- 英文仍为其它既定语言的明确回退值。日文及其它拉丁字母语言尚没有完整研究端译文，必须如实视为英文回退，不得标注为完整本地化。
- 本轮已执行 `git diff --check` 与 `npm run build`，均通过；构建仅有既有的前端包体积提示。发布后仍需由真实 Business 会话完成英文、简体中文、繁体中文、日文及一种拉丁字母语言的最小验收：个人/组织引导、两类简报、项目读取、问卷零答卷与已答卷结果、报价动作，以及所有跳转保留 `lang`。当前没有可安全使用的受控生产 Business 测试会话，不能以匿名页面或截图替代这一项。

**第十七阶段交付结论**

- 已完成并可发布：服务首页与两类服务定位、首次引导、项目简报、私有问卷草稿入口、真实结果索引/详情/空状态、项目与报价阅读流程、简体/繁体中文的可见文案与语言参数传递。
- 2026-09-19 已推送前端提交 `56c46d1`（`complete research workspace localization`）；正式域名已返回包含该提交独有的项目报价三语文案的前端包，说明当前静态页面已更新。该核验只证明已发布的客户端资源，不代替需要登录身份与真实项目数据的流程验收。
- 不属于本阶段且仍不可对外声称上线：自动 AI 建问卷或报告、自动研究结论、假数据图表、未经过人工确认的研究启动，以及完整的日文/其它语言人工翻译。
- 下一阶段只在完成一次受控真实账户验收后开启；验收发现的问题优先修复，不以增加新功能覆盖现有流程问题。

### 第十八阶段：真实 Business 会话端到端验收（已开启）

**唯一目标**

- 用一个可控的真实 Business 账户完成研究端两类服务的完整走查，并以观察到的实际结果而不是代码推断决定下一项产品建设。先修阻断使用的问题，再讨论 AI、报告或新的工作台能力。

**验收路径**

1. 英文、简体中文与繁体中文分别进入工作台；完成个人与组织引导，检查刷新后状态、导航、退出/再次登录和 `lang` 参数。
2. 分别保存一份定制问卷与一份定制研究简报；确认前者进入私有问卷草稿，后者只停留在研究委托流程，二者都不会在保存时启动招募。
3. 在受控问卷中检查零答卷、至少一份正常答卷与一份异常快速答卷：结果索引、百分比、逐份答卷和待复核口径应只读取真实数据。
4. 对定制研究验证提交审核、接收报价、接受/拒绝报价和返回项目列表；不触发真实付款、公开发布或招募。
5. 将每个发现的问题记录为可复现的路径、预期与实际结果；修复后重测。只有上述路径全通过，才允许选定下一项功能范围。

### 首页跨区域消费展示（已实现，待人工线上检查）

- 首页的可拖拽移动地球已调整为第三屏，顺序为首屏、研究服务、跨区域消费、联系团队；原位置不再重复渲染该区块。
- 文案聚焦“跨区域消费，让你的区域研究不再局部维艰”，以亚太、欧洲与中东为区域级表达；不公开合作方名称、国家级名单或运营细节。
- 地球画布内新增三个随正射投影旋转与拖拽的区域标点，而非静态地图叠图；简体、繁体和英文已提供对应文案，其它语言临时回退英文。已执行 `git diff --check` 与 `npm run build`。
- 2026-09-20 已将节点扩展为中国大陆、港澳、东亚与东南亚、中东、欧洲和美国，并补入“一带一路”代表国家及西欧、南欧、中欧、北欧主要国家；节点均以仅在可见半球显示的投影弧线连接。新版主张为“跨区域消费，让洞察共享，让发展共进”，强调跨区域机会、本地行动、进入、协同与持续发展，不使用“比较各地情况”的表述。

### 研究员访问页与公共导航（已实现，待人工线上检查）

- `/business/register` 已使用研究团队俯拍图，`/business/login` 已使用手机参与者图；两张用户提供素材已压缩到页面展示所需尺寸并纳入前端资源。左侧文字增加暖白半透明信息底，保证在图片明暗变化下仍可阅读。
- 公共桌面导航扩展到最大 1320px，并重新分配菜单与动作区间距；“中国市场洞察能力”保持单行，窄屏仍按既有断点进入折叠菜单。
- 组织介绍页 `/business` 继续作为“面向组织”的公开跳转页；加入入口中的组织/研究员行动按钮现在直接进入 `/business/login`，不再先绕到介绍页。首页、公共导航及页脚的组织入口仍保留 `/business`，便于访客先了解服务。
- 注册验证码改为随“创建账户”请求一次性校验与消耗，不再由浏览器先调用独立验证接口、再提交创建请求；后端仍兼容已经走过旧验证接口的页面。前端提交与后端提交须同时部署后再用全新验证码验收。

### Business 工作区（本地完成，待发布与真实账户验收）

- 登录后的研究工作区保留原有全宽格局、左上角 GuanyiSearch 大 G 与窄图标栏；在不改变品牌壳的前提下，整理概览、项目与真实问卷结果入口、账户菜单和通知入口。
- 概览区提供“新建研究”“Start with AI”“问卷设计”三个入口；项目数、草稿数、已收答卷和最近项目全部来自当前账户的真实工作区记录，不填充示例项目、虚构趋势或假分析图。
- `Start with AI` 当前是安全的准备入口：收集研究目标并带入现有简报表单；在用户提供并接入 AI API 前，不生成问题、结论或任何自动研究结果。
- 已执行 `npm run build` 与 `git diff --check`，均通过。下一步在发布后以真实 Business 账户核对：侧栏导航、三张行动卡、AI 目标带入简报，以及无项目/有项目状态。
- 2026-09-21 工作区视觉二次收口：保留左上角大 G 与窄图标栏；概览页、项目卡、统计卡及研究选择弹窗统一调整为暖白、墨绿、雾绿的低对比层级，细化边框与正文字重。右上角账户首字母块已替换为两色“欢迎回来 + 账户名”，并在上方带入首页英雄文案的后两行作为轻量提示。
- 2026-09-21 工作区视觉三次收口：问候标题调整为首页同源的两行编辑式层级——深色衬线问候语与浅灰斜体姓名；行动入口以墨绿、雾绿和暖砂色形成主次，真实统计与最近项目置于圆角色块中。未加入样例进度、虚构完成率或任何不来自账户记录的数据。

### 第十九阶段：账号、机会与可审查研究简报（已开启）

**阶段目标**

- 把注册、登录、邮箱验证、找回密码、退出和研究者工作台收成一条以服务端状态为准的闭环；前端成功提示不能替代真实账户、会话或项目记录。
- 把参与者机会、报名、入选/未入选、完成记录与结果可见性连成连续流程；只展示真实、已获准且符合画像的机会，不填充演示供给、虚构奖励或未授权结果。
- 第一项 AI 能力只做研究简报辅助：将研究者提供的自然语言目标整理为可编辑草案（研究问题、目标受众、建议方法、需补充信息与风险提示）。草案不自动提交、不自动建问卷、不自动招募、不伪造结论，必须由研究者明确确认后才能进入既有流程。
- 在接入模型服务前先固定 API 边界、输入校验、错误反馈、草案版本记录、人工确认和审计字段；个人资料、问卷回答和未获授权的研究数据不得发送到未声明的第三方服务。

**首个切片与验收顺序**

1. 先完成真实账号链路的代码级检查与受控浏览器验收：注册/验证码消费、登录/退出、找回密码、过期或重复验证码、错误状态、刷新后的会话恢复与研究者工作台入口。
2. 再逐条复核参与者机会读取、匹配、单次报名、状态变化、完成记录和结果权限；所有状态以服务端记录为准，未完成画像和无匹配供给使用明确空状态。
3. 建立仅返回草案的研究简报接口与前端确认交互：输入长度/字段校验、结构化错误、草案版本号、创建者与时间记录、取消/重试边界；未完成人工确认前不得创建或发布研究对象。
4. 每个切片完成前端构建、后端语法/流程检查、桌面与 390px 移动端关键路径复核；真实邮件或生产账户验收仅在具备受控测试账户和明确投递授权时执行。

**发布边界**

- 第十九阶段不包含自动研究结论、自动发布/招募、自动奖励、假数据图表、未经确认的模型输出或绕过现有内容/商业审核闸门的快捷路径。
- 前端、后端、数据库脚本与生产运行状态分别记录；只有代码已推送、对应数据库变更已幂等执行、应用已重启并完成关键路径核验，才可称为生产完成。

**当前启动记录（2026-09-22）**

- 已完成本阶段前置发布：前端品牌标题、描述、邮件素材与交接文档提交已推送至远端 `main`，最新提交为 `2b6b422`；后端本轮提交为 `520ba28`。
- 尚未在本阶段声称真实账号或生产邮件链路已验收；下一次工作从首个账号链路切片开始，并在发现阻断问题后先修复再扩展机会或 AI 范围。
- 2026-09-22：欢迎邮件运行代码仍引用旧图片 URL，而前端已存在最新版研究反馈图片。旧图片文件已从前端资源移除；旧 URL 在静态站点层兼容映射到最新版图片，确保未更新的邮件服务也能读取最新版。前端提交为 `9252225`。后端已准备将模板直接切换为新版 URL 并新增防回退校验，但该仓库尚需独立写入权限后才能发布；在此之前不得删除兼容映射。
- 2026-09-22：生产邮件服务运行在独立发布目录，不能仅更新后端工作目录。已在真实 PM2 运行目录部署最新版欢迎图片引用、基础/安全模板与校验；欢迎邮件使用最新图片。随后将邮件头改为纯 HTML 的 G 标记与 `guanyisearch` 文本，并将全屏纯白调整为低亮度暖灰/暖白，避免邮件客户端阻止远程品牌图时出现空框或刺眼白底。生产模板校验及本机/公网健康检查均通过。后端源码提交为 `3f689c3`，远端写入仍待恢复，服务器备份保留。
- 注册面板、参与者工作区及研究者工作区已加 `notranslate`、`translate="no"` 与 `data-translate="no"` 隔离。关键表单、验证码、工作区菜单及路由状态应使用站内语言资源，不依赖浏览器翻译或翻译扩展；现有错误边界继续在第三方 DOM 注入造成渲染异常时提供安全刷新入口。
- 2026-09-24：研究者工作区的 `Start with AI` 已从“输入后直接进入长简报表单”调整为项目级引导入口。客户先在现有输入弹窗描述业务问题，再进入 `/business/ai-brief`：页头使用 `guanyisearch / Projects / 问题标题` 面包屑，页面沿用研究端的暖白、雾绿、墨绿细线和无衬线功能层级，不复制外部产品的深色侧栏或品牌元素。
- 该页目前是**受控的本地引导原型，不是已接入模型服务**：它依次收集目标对象、市场、计划有效回复数和时间要求；右侧将字段标为“已记录 / 仍待补充”。页面明确说明没有联系参与者、没有报价、没有生成研究结论。仅当研究者完成引导并主动点选“保存研究简报”时，才调用现有项目草稿创建流程；不自动创建问卷、不自动提交、不启动招募。
- 本轮新增 `src/pages/BusinessAiBrief.jsx` 及 `/business/ai-brief` 路由，并调整 AI 起始弹窗为跳转至该引导页。所有站内返回与保存后跳转保留 `lang` 参数；英文与简体中文文案已随页面实现。`git diff --check` 与 `npm run build` 均通过；因当前没有可安全使用的受控 Business 登录会话，未将匿名访问页当作此登录后页面的视觉或流程验收，待真实会话补测桌面和 390px 窄屏。
- 2026-09-24：研究简报引导已加入独立的受控后端 AI 接口 `POST /api/business/ai-brief/guidance`。接口只接受研究目标及当前的受众、市场、样本、时间四项规划文字；仅商业账号可调用，五分钟内最多 12 次，调用超时为 20 秒。它不读取历史答卷或参与者资料，不创建项目、问卷、报价或招募，不把模型回复当作研究结论。
- 模型输出被限制为已校验的 JSON：一段简短回复、可选研究重点和可选规划提示。知识库是随代码版本控制的审核规则与模板，不代表使用客户资料训练基础模型；规则明确只支持异步线上问卷，不建议一对一视频访谈，不承诺样本可得性、代表性、价格、周期或结果，也不索取个人联系方式、证件、支付信息或文件。
- 前端优先调用该接口；服务未配置、不可用或回复无效时，会展示“AI 引导暂时不可用”，并退回相同的本地下一问，不把本地文案伪装成模型输出。页面加入提交文本处理提醒，要求用户不要输入联系方式、保密信息或文件。后端 `npm run verify:research-brief-assistant` 与前端 `npm run build` 均已通过，仍未使用真实密钥或实际模型调用做验收。
- 下一步先完成持久化草案版本、人工确认记录、审计事件与用户可见的数据处理/保留说明；然后以新建、未暴露的运行时密钥配置受控环境，使用非敏感测试文本执行一次端到端调用。此前不得声称 AI 已在生产可用，且不得将密钥写入仓库、前端包、交接文档或聊天记录。
- 2026-09-24：聊天引导补入浏览器会话恢复。用户从工作区输入问题后，未保存草稿的目标、字段与对话暂存于当前浏览器会话；刷新聊天页会恢复，而返回项目或成功保存项目会清除该临时草稿。该临时恢复不等于创建项目，也不会启动研究。前端提交 `dddd164` 已推送到 `main`；正式域名加载的新版静态包已包含 AI 引导与会话恢复标识。无受控 Business 登录会话，仍需用户完成一次实际点击与窄屏验收。
- 2026-09-24：根据实际使用反馈，AI 引导页移除大号“问题标题 + 右侧字段表”布局：`Start with AI` 现在直接进入现有研究端外壳内的聊天页，保留同源左侧图标导航、账户和通知入口。首屏改为对当前账户的简短问候“嗨，{姓名}，你想研究什么？”，输入框成为第一步；简报字段收为顶部的轻量进度及可展开详情，不再占据右侧大卡。
- 对话的本地降级会根据用户本次输入的文字自动使用中文或英文；后端 AI 请求也明确携带目标回复语言，且首个“研究目标”字段已纳入接口校验。API 密钥未在运行环境配置时，仍会显示明确的本地引导提示，不得误称模型已经在生产响应。前端构建和后端边界脚本均需在发布前再次通过。
- 2026-09-24：上述界面收口前端提交 `d705ab8` 已推送并由正式域名的新静态包确认加载；新版包包含“Hi, {name}. What would you like to research?”、新会话暂存键和工作区入口文案。后端受控研究助手提交为 `b20b3d2`，但后端远端 `express-prisma-main` 推送仍返回 403，故不应把模型 API 称为已发布或已响应。待恢复后端远端/生产部署权限后，配置全新、未在聊天中暴露的运行时密钥，再用非敏感文本验收中文回复与结构化输出。
- 2026-09-24：已直接部署生产后端 AI 引导的最小兼容补丁。生产活动目录的 `businessController` 实际版本落后于本地路由，整文件覆盖曾使健康检查短暂返回 502；已立即从服务器备份恢复，随后以生产路由为基准只加入受认证保护的 `/api/business/ai-brief/guidance`。PM2 重启后健康检查恢复 200；以非敏感中文测试文本验证模型返回中文结构化回复通过，未登录公网请求返回 401。生产目录中原有的邮件相关未提交改动未覆盖。后端远端 403 仍未解决，因此服务器补丁与远端 Git 当前不同步；下次后端发布必须以生产现有路由做三方核对，禁止整文件覆盖。
- 2026-09-24：AI 聊天页继续按实际操作反馈收口：移除首屏上方的私有草稿/进度排，将其改为右上“返回项目”下方的可展开“项目历史”；首屏问候置中，首次发送后自动隐藏并变为自然延展的对话记录。左侧研究端导航改为固定视口高度，页面整体滚动，不再因聊天内容增长而拉长导航栏。输入区采用白色分层渐变、背景模糊、边缘高光与低饱和反光的液态玻璃风格，仅用于输入控件以保留研究端可读性。`npm run build` 与 `git diff --check` 已通过，待正式静态包核验及用户已登录会话的实际滚动验收。
- 2026-09-24：首屏主文案改为不带称呼的“你想研究什么？/ What would you like to research?”；输入区上方新增随机轮换、打字机效果的研究灵感（中英各四条跨市场问卷示例）。它们是内置灵感文案，不能标为模型实时生成；模型只在用户主动发送内容后调用。用户首次发送研究问题后，左上品牌右侧展示 `/ 问题名称`。通知按钮的弹层改为页面视口居中显示。液态玻璃输入区降低边框、阴影与色彩反光强度，保留玻璃分层但不抢占研究内容。
- 2026-09-24：聊天超长的根因是模型常规返回了可选“研究重点/规划提示”，使每次回复出现多段文字。知识库已收紧为：最多两句（简短确认 + 一个下一问），除真正必要的澄清外两个可选字段必须为空；生产服务器已更新该规则并以中文非敏感样例验证通过。前端在新内容完成后调用页面级平滑滚动至输入区。首屏打字机移到主标题正下方、字号增大且速度放慢；问号保留但从衬线大标题中拆出，改为更小的无衬线字形，避免原字形的弯钩比例失衡。

### 第十九阶段：问卷编辑器第一层（2026-09-24）

- 问卷编辑器从单纯题目列表升级为可保存的问卷规格：每题可定义分段（筛选、核心、画像、收尾）、题型、必答、选项、可填写“其他”、多选上限、选项随机化、量表端点标签，以及“仅当上一题答案满足条件时展示”的显示规则。
- 本轮实际支持单选、多选、排序、评分、数字、短文本与长文本。公开答题端会按显示规则隐藏不适用题；隐藏题不会触发必答校验或写入答卷。排序、数字和“其他”答案均有服务端校验，已发布且已有答卷的问卷仍禁止修改结构。
- 编辑器加入发布前的基础设计检查：空题、选项不足、没有核心题、开放题过多、显示规则指向错误和超过 20 题会明确提示。它是辅助检查，不替代研究方法、跨市场可比性或人工审核。
- 前端提交 `0c7cd50` 已推送至 `main`；后端提交 `3ca199f` 因远端仍返回 403 未能推送。后端对应文件已按最小范围直接部署，幂等数据库兼容更新与服务健康检查均通过。未覆盖生产端的其他本地改动。前端正式域名在推送后的两次静态资源检查仍返回旧构建，因此暂不把编辑器前端称为已上线；需在托管发布记录或下一次构建完成后重新核验。
- 本地验证：前端 `npm run build`；后端语法检查与 `npm run verify:business-questionnaire`。后者覆盖设计字段保存、条件题可见性、排序答题、发布审核、真实答卷读取和结构锁定。仍需用受控 Business 会话完成编辑器桌面/窄屏交互与真实公开答题页的人工验收。
- 下一步不是堆砌更多题型，而是先补齐问卷预览、题目重排后规则重映射、逻辑流可视化、模板/版本草稿和跨语言问卷文本。跳题、终止页、配额与复杂配额/样本功能必须在对应校验、预览和人工审核完整后才实现。

### 第十九阶段：研究端统一入口与项目流（2026-09-24）

- 研究端左侧导航现将“仪表板、AI 研究引导、问卷编辑器、项目、结果”作为同一工作区的一级入口；AI 紧随仪表板，问卷编辑器不再只藏在项目卡内部。
- 问卷编辑器页展示当前账户真实的私有问卷草稿，并可新建草稿或进入已有编辑器。新建空白问卷后会直接进入对应项目的编辑器；AI 引导在研究者主动保存后同样创建一份私有问卷草稿并直接进入编辑器。AI 不自动编造题目、发布、联系参与者或生成研究结论。
- 仪表板增加问卷草稿、待确认范围等真实状态计数，以及“AI 起草／问卷编辑／定制研究 → 项目草稿与确认 → 报价与付款”的统一流程说明。最后一环明确标为后续接入：当前不提供自动估价、自动付款或伪造的报价结果。
- 前端 `npm run build` 与 `git diff --check` 已通过，提交 `4caeaf8` 已推送；正式站点的新静态包已确认包含问卷编辑器和统一项目流。仍需以受控 Business 会话验收：AI 保存后进入编辑器、空白问卷新建后进入编辑器、已有草稿列表和桌面/窄屏侧栏顺序。
- 2026-09-24 后续反馈：仪表板上的项目流程说明卡已移除，不再把内部流程作为面向研究者的页面内容。此前为避免浏览器翻译扩展改变 React 工作区 DOM，工作区根节点被整体标记为不可翻译，范围过大，导致无法翻译成中文；现已改为允许工作区页面使用浏览器翻译，并在左侧提供站内 `中 / EN` 切换。AI 页面保留对实时对话 DOM 的保护，但提供同一站内语言切换。
- 2026-09-24：自助问卷编辑器与定制问卷服务已从入口上拆分。左侧“问卷编辑器”中的“新建自助问卷”只收集名称、决策和受众三项最小信息，创建私有问卷草稿后直接打开题目编辑器；不再经过定制服务表单。定制问卷从“新建研究”进入 `/business/custom-questionnaire`，先展示服务说明弹窗，再用两页填写：`01 决策与受众`、`02 研究范围`。第二页将“参与者人数”表述为“预计样本数”，并将受众字段改为“你的受众群体是哪些”。
- 定制问卷弹窗使用用户提供的研究素材，文案说明提交后由团队审核、问卷方案可供用户查看和修改、范围确认后才进入采集与分析、完成后可在工作区查看/下载报告；不承诺自动报价、即时数据或未经确认的研究启动。前端构建通过，仍需受控 Business 会话验收两条路径分别落到“题目编辑器”与“项目审核草稿”。

## 尚未开发，不可对外表述为已上线

1. 用户投稿新闻、观点与文章的真实提交、审核与公开发布流程。
2. 公开社区评论、研究信号、内容展示与奖励机制。
3. 本地研究机构的合作档案、权限和联合活动流程。
4. 基于真实社区参与数据生成并发布的原创研究报告。
5. 按兴趣与语言生成的语音新闻播报。

## 低上下文续接规则

- 新对话先读取本文件，再只读取与当前任务直接相关的 1–3 个文件；不要回溯长历史。
- 用户提供截图时，只保留当前要改的区域和目标，不重复粘贴旧截图或整页前端代码。
- 一次只处理一个页面或一条用户流程；完成后先更新本文件，再开始下一个视觉方向。
- 发布前执行针对性构建；不要为了常规视觉调整重复进行大范围扫描。

## 推荐续接提示

`请先读取 docs/PROJECT_HANDOFF.md，从第十六阶段继续。不要回溯历史；先选定一个页面或一条真实流程，补齐语言资源和 lang 参数传递，完成最小验收后更新本交接文档。`
